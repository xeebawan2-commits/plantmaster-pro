{
  "name": "plantmaster-pro",
  "version": "4.49.0",
  "private": true,
  "type": "module",
  "description": "PlantMaster Pro — multi-plant maintenance management PWA",
  "scripts": {
    "build": "node scripts/build.mjs --all",
    "build:app": "node scripts/build.mjs --app",
    "build:site": "node scripts/build.mjs --site",
    "build:admin": "node scripts/build.mjs --admin",
    "dev": "node scripts/build.mjs --all && node scripts/serve.mjs",
    "verify": "npm run verify:db && npm run verify:pwa && npm run verify:links",
    "verify:db": "node tools/db-verify/verify.mjs",
    "verify:pwa": "node scripts/build.mjs --app && node scripts/verify-pwa.mjs",
    "verify:links": "node scripts/verify-links.mjs",
    "deploy": "npm run build:app && wrangler pages deploy public --project-name=plantmaster-app",
    "deploy:site": "npm run build:site && wrangler pages deploy dist/site --project-name=plantmaster-site",
    "deploy:admin": "npm run build:admin && wrangler pages deploy dist/admin --project-name=plantmaster-admin"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "wrangler": "^4.13.0"
  }
}
